import UIKit

/// Legacy file retained in the target for compatibility. The app is now driven
/// by the SwiftUI `HangTrackerApp` entry point; this view controller is no
/// longer instantiated (the Main storyboard reference was removed from
/// Info.plist). Kept as an empty placeholder to avoid churn in the project file.
class ViewController: UIViewController {
    // Intentionally empty.
}
